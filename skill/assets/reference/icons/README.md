Drop single-object monoline Notion-style icon references here (24px grid, uniform stroke).
A free 50-icon Notion-style set exists; a real `.svg` is worth more than any PNG.
Style calibration only — never copy an icon's geometry into output.
